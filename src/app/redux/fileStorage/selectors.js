export const selectData = (state) => state.fileStorage.data;
export const selectCurrentData = (state) => state.fileStorage.current;
export const selectIsLoading = (state) => state.fileStorage.isLoading;
export const selectError = (state) => state.fileStorage.error;
